# Hugging Face token used for hosted inference (never commit the real value).
HF_TOKEN=hf_your_token_here

# Model called via Hugging Face Inference Providers.
MODEL_ID=Qwen/Qwen2.5-1.5B-Instruct

# CORS: your production frontend origin (optional for local dev).
# FRONTEND_URL=https://your-app.vercel.app
